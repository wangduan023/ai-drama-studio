# AI Drama Studio 代码审查报告

**审查日期**: 2026-03-11  
**审查范围**: 全代码库（Phase 1 认证系统 + Phase 2 AI生成系统 + Phase 3 团队协作）  
**审查人员**: Kimi Code CLI

---

## 执行摘要

本次审查对 AI Drama Studio 项目的三个主要阶段进行了全面的代码质量检查。项目功能完整但存在一些需要关注的安全问题和代码质量问题。

### 审查统计
| 阶段 | 严重问题 | 中等问题 | 建议 |
|------|---------|---------|------|
| Phase 1 认证系统 | 3 | 5 | 10 |
| Phase 2 AI生成系统 | 2 | 12 | 8 |
| Phase 3 团队协作 | 4 | 7 | 5 |
| **总计** | **9** | **24** | **23** |

### 修复状态
- **已修复**: 4 个严重问题（Phase 1 全部修复）
- **待修复**: 5 个严重问题（详见下文）

---

## Phase 1: 本地认证系统 - 已修复 ✅

### 已修复问题

#### 1. ✅ JWT 密钥硬编码默认值 [SECURITY]
**位置**: `apps/web/lib/auth/local.ts`, `apps/web/lib/auth/session.ts`
**修复**: 移除默认值，添加环境变量强制检查

#### 2. ✅ API 路由硬编码 userId [SECURITY]
**位置**: 8 个 API 路由文件
**修复**: 使用 verifyAuth() 从 JWT token 验证获取真实用户ID

#### 3. ✅ 密码生成不安全 [SECURITY]
**位置**: `apps/web/lib/auth/password.ts`
**修复**: 使用 crypto.randomBytes 替代 Math.random

#### 4. ✅ Playwright 端口配置
**位置**: `playwright.config.ts`
**修复**: URL 端口从 3000 改为 3333

---

## Phase 2: AI内容生成系统 - 待修复

### 🔴 严重问题（需立即修复）

#### 1. 积分扣除竞态条件 [SECURITY]
**位置**: `apps/web/lib/credits/index.ts` 第 76-142 行
**问题**: 虽然使用了 Prisma 事务，但没有设置事务隔离级别，可能存在幻读问题
**修复**:
```typescript
await prisma.$transaction(async (tx) => {
  // 事务代码
}, {
  isolationLevel: Prisma.TransactionIsolationLevel.Serializable
});
```

#### 2. 异步任务轮询逻辑未完成
**位置**: 
- `apps/worker/src/handlers/imageGenerateHandler.ts` 第 146-168 行
- `apps/worker/src/handlers/videoGenerateHandler.ts` 第 352-378 行
**问题**: 异步图片/视频生成的轮询逻辑只是模拟，没有实际查询状态
**修复**: 实现实际的异步状态查询逻辑

### 🟡 中等问题

#### 3. API Key 安全
- **位置**: `packages/ai-client/src/base.ts`, `multi-account-balancer.ts`
- **问题**: API Key 存储在内存中，没有安全清理机制；generateAccountName 暴露 API Key 后 8 位
- **建议**: 实现 secureDispose 方法，使用哈希生成标识符

#### 4. 成本计算硬编码汇率
- **位置**: `packages/ai-client/src/utils/cost-calculator.ts`
- **问题**: USD/CNY 汇率固定为 0.139，可能不准确
- **建议**: 实现可配置的汇率系统

#### 5. TOCTOU 竞态条件
- **位置**: `apps/web/lib/credits/index.ts` 第 286-300 行
- **问题**: hasEnoughCredits 先检查再扣款存在竞态条件
- **建议**: 移除此函数，所有扣款在事务中进行并捕获错误

#### 6. 成本配置硬编码
- **位置**: `apps/web/lib/credits/cost.ts`
- **问题**: 成本配置无法动态调整
- **建议**: 从数据库读取配置

---

## Phase 3: 团队协作功能 - 待修复

### 🔴 严重问题（需立即修复）

#### 1. 项目 API 权限检查不支持协作者 [SECURITY]
**位置**: 
- `apps/web/app/api/projects/[id]/route.ts`
- `apps/web/app/api/projects/route.ts`
**问题**: 只检查 project.userId，没有考虑 ProjectMember
**修复**:
```typescript
// 项目列表查询
where: {
  OR: [
    { userId, deletedAt: null },
    { members: { some: { userId } }, deletedAt: null }
  ]
}
```

#### 2. WebSocket 房间未授权访问 [SECURITY]
**位置**: `apps/web/lib/collaboration/socket.ts` 第 68-99 行
**问题**: join-project 事件没有验证用户是否有项目访问权限
**修复**:
```typescript
socket.on('join-project', async (projectId: string) => {
  const hasAccess = await canViewProject(userId, projectId);
  if (!hasAccess) {
    socket.emit('error', { message: 'Access denied' });
    return;
  }
  // ...
});
```

#### 3. PermissionGate 硬编码权限 [SECURITY]
**位置**: `apps/web/components/collaboration/PermissionGate.tsx` 第 27-46 行
**问题**: 使用 `setRole('EDITOR')` 硬编码，所有用户都被视为 EDITOR
**修复**: 实现真实的 API 调用获取角色

#### 4. 邀请 Token 泄露
**位置**: `apps/web/app/api/projects/[id]/invite/route.ts` 第 164-175 行
**问题**: 活动日志中记录了完整的邀请 token
**修复**: 只记录 token 前缀或哈希值

### 🟡 中等问题

#### 5. 项目列表不包含协作项目
- **位置**: `apps/web/app/api/projects/route.ts`
- **问题**: 协作者无法在项目列表中看到被邀请的项目
- **建议**: 查询包含 members 关联的项目

#### 6. 评论 XSS 风险
- **位置**: `apps/web/components/collaboration/CommentItem.tsx`
- **问题**: 用户输入直接渲染
- **建议**: 使用 DOMPurify 清理内容

#### 7. 成员移除记录错误
- **位置**: `apps/web/app/api/projects/[id]/members/[userId]/route.ts`
- **问题**: removedUserEmail 错误地使用了 userId
- **建议**: 使用 member.user?.email

---

## 数据库模型问题

### 🔴 错误

#### 1. ProjectActivity.userId 缺少外键关系
**位置**: `schema.prisma` ProjectActivity 模型
**修复**: 添加关系定义
```prisma
user User? @relation(fields: [userId], references: [id], onDelete: SetNull)
```

### 🟡 警告

#### 2. Float 类型存储金额
**位置**: Credit, CreditTransaction 模型的 balance/amount 字段
**问题**: 存在精度问题（0.1 + 0.2 ≠ 0.3）
**修复**: 使用 Int（最小单位）或 Decimal 类型

#### 3. ProjectComment 级联删除策略
**位置**: ProjectComment parent 关系
**问题**: 父评论删除时子评论变成顶级评论
**修复**: 改为 `onDelete: Cascade` 或阻止删除

### 🟢 建议

#### 4. 索引优化
- CreditTransaction: 添加 `@@index([userId, createdAt])`
- ProjectComment: 添加 `@@index([userId])`, `@@index([projectId, episodeId])`
- ProjectInvite: 添加 `@@index([email])`, `@@index([expiresAt])`
- ProjectActivity: 添加 `@@index([projectId, action])`

#### 5. 字段补充
- Credit 模型添加 `updatedAt DateTime @updatedAt`
- ProjectMember.invitedBy 建立外键关联

---

## 修复优先级

| 优先级 | 问题 | 文件 | 估计工时 |
|--------|------|------|----------|
| P0 | WebSocket 房间权限检查 | socket.ts | 1 小时 |
| P0 | PermissionGate 硬编码 | PermissionGate.tsx | 30 分钟 |
| P0 | 项目 API 权限检查 | projects/route.ts | 1 小时 |
| P0 | 积分事务隔离级别 | credits/index.ts | 30 分钟 |
| P1 | 邀请 Token 泄露 | invite/route.ts | 30 分钟 |
| P1 | Float 金额类型 | schema.prisma | 1 小时 |
| P1 | ProjectActivity 外键 | schema.prisma | 30 分钟 |
| P2 | 异步轮询逻辑 | worker handlers | 4 小时 |
| P2 | 评论 XSS 防护 | CommentItem.tsx | 30 分钟 |
| P2 | 索引优化 | schema.prisma | 1 小时 |

---

## 总体评价

### 优势
1. **功能完整**: 三个阶段的开发目标均已实现
2. **架构清晰**: 模块化设计，职责分离明确
3. **类型安全**: TypeScript 覆盖率高
4. **文档完善**: 组件和工具都有使用文档

### 需改进
1. **权限检查**: 多处权限检查不完整或缺失
2. **安全意识**: 敏感信息处理和 XSS 防护需要加强
3. **竞态条件**: 积分扣除等关键操作需要更好的并发控制
4. **测试覆盖**: 关键业务逻辑需要更多单元测试

---

## 附录：文件变更统计

| 类别 | 新增文件 | 修改文件 | 删除文件 |
|------|---------|---------|---------|
| Phase 1 | 28 | 10 | 0 |
| Phase 2 | 45 | 8 | 0 |
| Phase 3 | 35 | 5 | 0 |
| **总计** | **108** | **23** | **0** |

---

*报告生成时间: 2026-03-11*  
*最后更新: 2026-03-11*
