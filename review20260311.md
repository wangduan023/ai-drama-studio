# AI Drama Studio 代码审查报告

**审查日期**: 2026-03-11  
**审查范围**: 全代码库（数据库模型、API路由、前端组件、测试文件、配置文件）  
**审查人员**: Kimi Code CLI

---

## 执行摘要

本次审查对 AI Drama Studio 项目进行了全面的代码质量检查。项目在整体架构、类型安全和组件设计方面表现良好，但发现了一些需要关注的安全问题和代码质量问题。

### 审查统计
- **严重问题**: 3 个（安全问题）
- **中等问题**: 10+ 个
- **建议改进**: 20+ 个

---

## 严重问题（需立即修复）

### 1. JWT 密钥硬编码默认值 [SECURITY]

**位置**: 
- `apps/web/lib/auth/local.ts` 第 15 行
- `apps/web/lib/auth/session.ts` 第 13 行

**问题描述**: 
代码中使用了硬编码的默认 JWT 密钥 `'default-secret-key'`，如果环境变量未设置，系统将使用不安全的密钥，攻击者可以轻易伪造 JWT 令牌。

```typescript
const JWT_SECRET = process.env.JWT_SECRET || process.env.NEXTAUTH_SECRET || 'default-secret-key';
```

**安全风险**: 攻击者可以使用默认密钥伪造任意用户身份，完全绕过认证系统。

**修复建议**:
```typescript
const JWT_SECRET = process.env.JWT_SECRET || process.env.NEXTAUTH_SECRET;
if (!JWT_SECRET) {
  throw new Error('JWT_SECRET or NEXTAUTH_SECRET must be set in environment variables');
}
```

---

### 2. 多处硬编码 userId [SECURITY]

**位置**: 
- `apps/web/app/api/characters/[id]/route.ts` 第 13, 118, 243 行
- `apps/web/app/api/characters/route.ts` 第 14, 132 行
- `apps/web/app/api/episodes/[id]/route.ts` 第 12, 115, 217 行
- `apps/web/app/api/episodes/route.ts` 第 12, 112 行
- `apps/web/app/api/locations/[id]/route.ts` 第 13, 74, 155 行
- `apps/web/app/api/locations/route.ts` 第 14, 104 行
- `apps/web/app/api/projects/[id]/route.ts` 第 13, 76, 154 行
- `apps/web/app/api/projects/route.ts` 第 13, 78 行

**问题描述**:
API 路由中硬编码了测试用户 ID `b29b8e81-d968-4563-9998-fc221137e842`，这会导致任何请求都被当作该用户处理，完全破坏了用户数据隔离。

```typescript
const userId = 'b29b8e81-d968-4563-9998-fc221137e842'
```

**安全风险**: 任何用户都可以访问和修改其他用户的数据。

**修复建议**:
```typescript
// 从 session 或 JWT token 获取真实用户ID
const { user } = await verifyAuth(request);
const userId = user?.id;

if (!userId) {
  return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
}
```

---

### 3. 密码生成使用不安全的随机数 [SECURITY]

**位置**: `apps/web/lib/auth/password.ts` 第 147-168 行

**问题描述**:
`generateRandomPassword` 函数使用 `Math.random()` 生成密码，这不是加密安全的随机数生成器。

```typescript
// 不安全的实现
password += chars.charAt(Math.floor(Math.random() * chars.length));
```

**安全风险**: 生成的密码可预测性高，容易被暴力破解。

**修复建议**:
```typescript
import { randomBytes } from 'crypto';

export function generateRandomPassword(length: number = 16): string {
  const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*';
  const bytes = randomBytes(length);
  let password = '';
  for (let i = 0; i < length; i++) {
    password += chars[bytes[i] % chars.length];
  }
  return password;
}
```

---

## 中等问题

### 4. Playwright 端口配置不一致

**位置**: `apps/web/playwright.config.ts` 第 96-101 行

**问题描述**:
webServer 配置中的 URL (`http://localhost:3000`) 与 dev 命令使用的端口 (`3333`) 不一致。

```typescript
webServer: {
  command: 'next dev -p 3333 -H 0.0.0.0',
  url: 'http://localhost:3000', // 应该是 3333
}
```

**影响**: E2E 测试无法正确等待服务启动。

**修复建议**:
```typescript
webServer: {
  command: 'next dev -p 3333 -H 0.0.0.0',
  url: 'http://localhost:3333',
  reuseExistingServer: !process.env.CI,
  timeout: 120 * 1000,
}
```

---

### 5. 图片域名配置过于宽松

**位置**: `apps/web/next.config.ts` 第 34-46 行

**问题描述**:
图片配置允许所有 HTTPS 域名 (`hostname: '**'`)，存在安全风险。

```typescript
{
  protocol: 'https',
  hostname: '**', // 过于宽松
}
```

**修复建议**: 限制为实际使用的 CDN 域名。

---

### 6. API 代理地址硬编码

**位置**: `apps/web/next.config.ts` 第 56-62 行

**问题描述**:
API 代理硬编码为 `localhost:3001`，在生产环境不适用。

**修复建议**: 从环境变量读取目标地址。

---

### 7. E2E 测试过度使用固定等待时间

**位置**: 所有 `__tests__/e2e/*.spec.ts` 文件

**问题描述**:
大量使用 `page.waitForTimeout(500)` 等固定等待时间，导致测试不稳定且运行缓慢。

**修复建议**: 使用 Playwright 的自动等待机制。
```typescript
// 不推荐
await page.waitForTimeout(500);

// 推荐
await page.waitForResponse(response => response.url().includes('/api/characters'));
```

---

### 8. 测试辅助函数重复定义

**位置**: `__tests__/e2e/characters.spec.ts`, `episodes.spec.ts`, `locations.spec.ts`

**问题描述**:
`createTestProject` 和 `cleanupTestData` 辅助函数在多个文件中重复定义。

**修复建议**: 提取到共享工具文件。
```typescript
// __tests__/e2e/helpers/test-helpers.ts
export async function createTestProject(page: Page, name: string): Promise<string> { ... }
```

---

### 9. 环境变量示例文件使用看似真实的密钥

**位置**: `.env.example`

**问题描述**:
示例文件中包含看似真实的密钥值，可能被误用于生产环境。

```bash
NEXTAUTH_SECRET="ai-drama-studio-default-secret-2026"
```

**修复建议**: 使用明显的占位符。
```bash
NEXTAUTH_SECRET="your-secret-key-here-min-32-chars-long"
```

---

### 10. AiUsageLog 模型缺少外键关系

**位置**: `packages/db/prisma/schema.prisma` 第 184-226 行

**问题描述**:
AiUsageLog 模型定义了 projectId、userId 字段，但没有定义外键关系，无法在数据库层面保证引用完整性。

**修复建议**:
```prisma
model AiUsageLog {
  // ... 其他字段
  project   Project? @relation(fields: [projectId], references: [id], onDelete: SetNull)
  user      User?    @relation(fields: [userId], references: [id], onDelete: SetNull)
}
```

---

### 11. 依赖版本不一致

**位置**: 根目录和 apps/web 的 package.json

**问题描述**:
根目录和 apps/web 都安装了 next、react、react-dom，版本不一致。
- 根目录 Next.js: ^15.1.0
- apps/web Next.js: ^15.5.7

**修复建议**: 统一版本，或使用 pnpm workspaces 的 catalog 功能管理依赖。

---

## 建议改进

### 代码组织和可维护性

1. **CORS 函数重复定义**: 将 `corsHeaders` 提取到公共模块 `@/lib/cors`
2. **硬编码配置**: 将导航项、步骤配置等提取到常量文件
3. **类型定义**: 为所有 API 响应定义严格的类型
4. **国际化**: 所有中文文本使用 i18n 方案管理

### 性能优化

1. **QueryClient 全局实例**: 在服务端渲染时使用 `cache()` 函数创建新实例
2. **useEffect 依赖**: 修复多个 hooks 中缺失或不正确的依赖数组
3. **防抖处理**: 自动保存功能添加防抖

### 可访问性

1. **按钮 aria 标签**: 为图标按钮添加 `aria-label`
2. **角色属性**: 为可点击卡片添加正确的 `role` 和 `aria-pressed`
3. **当前页面标记**: 使用 `aria-current="page"` 标记当前页面

### 测试改进

1. **等待机制**: 替换所有 `waitForTimeout` 为自动等待
2. **测试数据**: 使用 API 创建测试数据，避免硬编码 ID
3. **覆盖率阈值**: 适当提高测试覆盖率阈值
4. **afterEach 导入**: 修复 `api.test.ts` 缺少 `afterEach` 导入

---

## 正面评价

### 架构设计
- 项目整体架构清晰，使用 Next.js App Router
- 组件设计遵循单一职责原则
- 类型定义完善，TypeScript 覆盖率高

### 认证系统
- 新实现的认证系统功能完整
- 密码哈希使用 bcrypt，salt rounds 设置合理
- JWT 令牌有过期时间控制
- 支持记住我功能

### 测试覆盖
- 409 个测试（171 单元 + 238 集成）全部通过
- 20 个 E2E 核心测试通过
- 测试文件组织清晰

### 代码质量
- 使用 ESLint 和 Prettier 进行代码规范
- 组件使用 Tailwind CSS + shadcn/ui，样式一致
- 错误处理完善，有统一的错误响应格式

---

## 修复优先级

| 优先级 | 问题 | 估计工时 |
|--------|------|----------|
| P0 | JWT 密钥硬编码 | 1 小时 |
| P0 | 硬编码 userId | 2 小时 |
| P0 | 密码生成不安全 | 30 分钟 |
| P1 | Playwright 端口配置 | 15 分钟 |
| P1 | 图片域名配置 | 30 分钟 |
| P1 | E2E 测试 waitForTimeout | 3 小时 |
| P2 | 提取重复辅助函数 | 1 小时 |
| P2 | 依赖版本统一 | 30 分钟 |
| P2 | 环境变量示例 | 15 分钟 |

---

## 附录：详细问题清单

### 数据库模型
- [ ] AiUsageLog 缺少外键关系
- [ ] AiUsageLog 缺少 userId 索引
- [ ] JSON 字段类型不一致（String? vs Json?）
- [ ] RefreshToken token 字段没有长度限制
- [ ] Soft delete 的 deletedBy 没有建立关系

### API 路由
- [ ] 所有 API 路由硬编码 userId
- [ ] CORS 函数重复定义
- [ ] JSON.parse 缺少错误处理
- [ ] SSE 认证仅依赖可伪造的 header

### 前端组件
- [ ] useEffect 依赖数组问题
- [ ] 硬编码配置数据
- [ ] 未使用的导入
- [ ] 可访问性属性缺失

### 测试
- [ ] api.test.ts 缺少 afterEach 导入
- [ ] 大量 waitForTimeout 使用
- [ ] 辅助函数重复定义
- [ ] 硬编码测试数据

### 配置
- [ ] JWT 密钥硬编码默认值
- [ ] Playwright 端口不一致
- [ ] 图片域名过于宽松
- [ ] API 代理地址硬编码
- [ ] 环境变量示例使用真实值
